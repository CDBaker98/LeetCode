/*
Problem 50:
Implement pow(x, n), which calculates x raised to the power n (i.e., x^n).
*/

public class Solution {
    public double MyPow(double x, int n) {
        return Math.Pow(x, n); // I'm not against using the tools I have available
    }
}
